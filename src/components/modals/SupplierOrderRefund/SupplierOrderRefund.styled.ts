import styled from '@emotion/styled'

import { Modal } from 'ui'

export const SupplierOrderRefundModal = styled(Modal)``

export const Form = styled.form`
  display: flex;
  flex-direction: column;
  gap: 20px;
`
